package strategy;

public enum SearchStrategyType {
    PRICE("PRICE"),
    ROOM_TYPE("ROOM_TYPE"),
    SIZE("SIZE"),
    LOCATION("LOCATION"),
    LISTING_TYPE("LIST_TYPE");

    SearchStrategyType(String s) {
    }
}
