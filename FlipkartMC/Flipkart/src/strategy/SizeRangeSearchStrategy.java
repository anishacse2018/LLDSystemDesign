package strategy;

import model.Property;

import java.util.List;

public class SizeRangeSearchStrategy implements SearchStrategy{

    @Override
    public List<Property> searchProperties(List list, Object searchField) {
        return List.of();
    }
}
